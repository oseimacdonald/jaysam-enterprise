<nav>
    <div class="container">
        <div class="nav-content">
            <!-- Desktop Navigation -->
            <ul class="desktop-nav">
                <!-- Home - Always visible -->
                <li><a href="/" title="Home page">Home</a></li>
                
                <!-- Products - Everyone can see -->
                <li><a href="/products" title="Browse Products">Products</a></li>
                
                <!-- Cart - Everyone can see (with counter) -->
                <li>
                    <a href="/cart" title="Shopping Cart" class="position-relative">
                        Cart
                        <span class="cart-badge position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" id="desktopCartCount" style="display: none;">
                            0
                        </span>
                    </a>
                </li>
                
                <!-- Orders - Only logged-in employees+ can see -->
                <% if (isEmployee) { %>
                    <li><a href="/orders" title="My Orders">Orders</a></li>
                <% } %>
                
                <!-- Customers - Only managers+ can see -->
                <% if (isManager) { %>
                    <li><a href="/customers" title="Customer Management">Customers</a></li>
                <% } %>
                
                <!-- Account Management - Only admins can see -->
                <% if (isAdmin) { %>
                    <li><a href="/account/" title="Account Management">Accounts</a></li>
                <% } %>
                
                <!-- Login/Logout -->
                <% if (isLoggedIn) { %>
                    <li><a href="/account/logout" title="Logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/account/login" title="Login">Login</a></li>
                <% } %>
            </ul>

            <!-- Hamburger Menu Button -->
            <button class="hamburger-menu" id="hamburgerMenu">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </div>
</nav>

<!-- Mobile Navigation -->
<div class="mobile-nav" id="mobileNav">
    <ul>
        <!-- Home - Always visible -->
        <li><a href="/" title="Home page">Home</a></li>
        
        <!-- Products - Everyone can see -->
        <li><a href="/products" title="Browse Products">Products</a></li>
        
        <!-- Cart - Everyone can see (with counter) -->
        <li>
            <a href="/cart" title="Shopping Cart" class="position-relative">
                Cart
                <span class="cart-badge position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" id="mobileCartCount" style="display: none;">
                    0
                </span>
            </a>
        </li>
        
        <!-- Orders - Only logged-in employees+ can see -->
        <% if (isEmployee) { %>
            <li><a href="/orders" title="My Orders">Orders</a></li>
        <% } %>
        
        <!-- Customers - Only managers+ can see -->
        <% if (isManager) { %>
            <li><a href="/customers" title="Customer Management">Customers</a></li>
        <% } %>
        
        <!-- Account Management - Only admins can see -->
        <% if (isAdmin) { %>
            <li><a href="/account/" title="Account Management">Accounts</a></li>
        <% } %>
        
        <!-- Login/Logout -->
        <% if (isLoggedIn) { %>
            <li><a href="/account/logout" title="Logout">Logout</a></li>
        <% } else { %>
            <li><a href="/account/login" title="Login">Login</a></li>
        <% } %>
    </ul>
</div>

<!-- Overlay -->
<div class="nav-overlay" id="navOverlay"></div>